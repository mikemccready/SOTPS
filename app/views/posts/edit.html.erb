<h1>Editing Post</h1>

<% if current_user.id == 5 %>

	<%= @post.user_id %>
	<%= current_user.id %>

	<%= render 'form' %>

<% end %>	
	

<%= link_to 'Show', @post %> |
<%= link_to 'Back', posts_path %>
